{
  "scripts": {
    "build:excalidraw": "esbuild vendor-src/excalidraw-preview.jsx --bundle --format=esm --splitting --outdir=vendor --conditions=production --loader:.woff2=file --jsx=automatic --minify",
    "dev": "node server.cjs"
  },
  "dependencies": {
    "@excalidraw/excalidraw": "^0.18.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "sharp": "^0.35.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
